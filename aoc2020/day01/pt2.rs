use std::collections::HashSet;

fn main() {
    let input = include_str!("input");
    let nums = input.lines().collect::<Vec<&str>>();
    const TARGET: u32 = 2020;
    for i in 0..(nums.len() - 2) {}
    let x = 3;
    x = "str";
}
